#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'The program description'
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
